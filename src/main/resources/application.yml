

# server port config
server:
  port: 8080  # server port

# db config
spring:
  jwt:
    secret: vmfhaltmskdlstkfkdgodyroqkfwkdbalroqkfwkdbalaaaaaaaaaaaaaaaabbbbb
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver         # db driver
    url: jdbc:mysql://localhost:3306/bu_200_groupware # db url
    username: root                                # db username
    password: root                      # db password
  devtools:
    restart:
      enabled: false # auto restart option enabled

  # jpa config
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

# Logging Level
logging:
  level:
    root: info




